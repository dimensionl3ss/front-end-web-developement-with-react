import { DISHES } from '../shared/dishes';

export const Dishes = (state = DISHES, action) => { //simpler reducer function
    switch (action.type) {
        default:
          return state;
      }
};